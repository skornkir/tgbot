// modules/cmd.help.js

const isAdminChat = require('../admin/permissionAdminChat');

module.exports = function (bot) {
  bot.onText(/^!команды$/i, async (msg) => {
    const chatId = msg.chat.id;
    const isAdminChatPermission = await isAdminChat(chatId);

    if (isAdminChatPermission) {
      // ===== Панель администратора (HTML: только <b> и <code>) =====
      const adminText = [
        '🛡️ <b>Панель администратора — команды</b>',
        '',
        '<b>Изменение описания (по тегу):</b>',
        '• <code>+ник @тегИгрока новыйНик</code> — меняет ник заданного игрока.',
        '• <code>+возраст @тегИгрока 21</code> — меняет возраст заданного игрока.',
        '• <code>+город @тегИгрока Город</code> — меняет город заданного игрока.',
        '• <code>+имя @тегИгрока Имя</code> — меняет имя заданного игрока.',
        '• <code>+id @тегИгрока 123456789</code> — меняет PUBG ID заданного игрока.',
        '• 📝 <code>+заметка @тегИгрока текст</code> — меняет заметку заданного игрока.',
        '',
        '<b>Кланы и слоты:</b>',
        '• <code>!инвайтN</code> — получить инвайт-код в клан №N.',
        '• <code>!места</code> — показать лимиты и свободные места по подкланам текущего клана.',
        '• <code>!списокN</code> — список сокланов клана №N.',
        '• <code>+подклан @тегНовогоЛидера</code> — создать подклан и назначить лидера.',
        '• <code>+ссылка N https://t.me/...</code> — установить ссылку чата клана №N.',
        '• <code>+лимит N M</code> — установить лимит M для клана №N.',
        '• <code>+клан @тегИгрока N</code> — перевести соклана в клан №N.',
        '',
        '<b>Модерация:</b>',
        '• <code>!бан @тегИгрока причина…</code> — бан во всех чатах клана (причину дописать после тега).',
        '• <code>!разбан @тегИгрока</code> — снять бан.',
        '• <code>!баны</code> — список забаненных.',
        '• <code>+модерация @тег N</code> — добавить модератора в клан',
        '• <code>-модерация @тег</code> — убрать модератора',
        '• <code>!сменить лидера @тег N</code> — меняет лидера клана, где @тег - юз игрока, а N - номер клана',
        '• <code>!мут @тегИгрока 10мин</code> или <code>!мут @тегИгрока 2ч</code> — запрет писать на время.',
        '',
        '<b>Статистика:</b>',
        '• <code>!активность @тегИгрока</code> — активность заданного игрока.',
        '',
        '<b>Чаты:</b>',
        '• <code>!привязать чат</code> — привязать текущий чат к клану.',
        '  Можно только создателю, который регистрировал клан.',
        '• <code>+объвление</code> — в следующем сообщении текст объявления. оно разошлется всем вашим сокланам',
        '',
        '<b>Правила:</b>',
        '• <code>+правила</code> — в следующем сообщении текст правил; установить/обновить правила клана.',
        '',
        '<b>Турниры:</b>',
        '• <code>+турнир N Название</code> — создать турнир (N — размер команды).',
        '• <code>+дата турнира 20.10.25 15:00</code> — установить дату проведения (формат: ДД.ММ.ГГ ЧЧ:ММ).',
        '• <code>!завершить турнир</code> — завершить активный турнир.',
        '• <code>+лобби текстСообщения</code> — делает рассылку сообщения через бота только участникам активного турнира',

      ].join('\n');

      await bot.sendMessage(chatId, adminText, {
        parse_mode: 'HTML',
        disable_web_page_preview: true
      });
      return;
    }

    // ===== Обычные команды (HTML: только <b> и <code>) =====
    const userText = [
      '📖 <b>Список доступных команд:</b>',
      '',
      '▫️ <b>описание</b> — ваше описание.',
      'В ответ на чужое сообщение — описание того, кому вы отвечаете.',
      '▫️ <b>описание @тег</b> — описание человека по тегу.',
      '',
      '✏️ <b>Изменение описания (для себя):</b>',
      '• <code>+ник</code> — меняет ваш ник.',
      '• <code>+возраст</code> — меняет ваш возраст.',
      '• <code>+город</code> — меняет ваш город.',
      '• <code>+имя</code> — меняет ваше имя (если заполнено неправильно).',
      '• <code>+id</code> — меняет ваше id.',
      '• 📝 <code>+заметка</code> — меняет вашу заметку.',
      '',
      '💍 <b>Браки:</b>',
      '• <code>!браки</code> — список браков текущего чата.',
      '• <code>!брак @тег</code> — предложение вступления в брак.',
      '• <code>!развод</code> — расторжение отношений.',
      '',
      '📜 <b>Клан:</b>',
      '• <b>правила</b> — правила клана.',
      '• <code>!земляк</code> — сокланы в радиусе 400 км от вашего города.',
      '',
      '<b>Модерация:</b>',
       '• <code>!мой лидер</code> — список модераторов и лидера вашего клана.',
       '• <code>!модерация</code> — список всех модеров и лидеров.',
      '',
      '🏆 <b>Турнир (для участников):</b>',
      '• <code>+каста</code> — создать команду (после команды отправь PUBG ID участников, каждый с новой строки).',
      '  Пример:',
      '  <code>+каста</code>',
      '  <code>48494949</code>',
      '  <code>59494993</code>',
      '  <code>2191929</code>',
      '  <code>4949939222</code>',
      '• <code>-каста</code> — удалить свою команду (может любой участник команды).',
      '• <code>!участники</code> — список участников турнира.',

    ].join('\n');

    await bot.sendMessage(chatId, userText, {
      parse_mode: 'HTML',
      disable_web_page_preview: true
    });
  });
};
